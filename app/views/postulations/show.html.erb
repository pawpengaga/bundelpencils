<%=link_to 'Download as Excel', client_postulation_path(@postulation.client, @postulation, format: :xlsx)%>

<%= render @postulation %>

<div>
    <%= link_to "Volver", client_postulations_path(current_client), class:"btn mybtn mt-4" %>
</div>
